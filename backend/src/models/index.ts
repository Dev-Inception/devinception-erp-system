// Mongoose models live here. Define one file per entity and re-export below.
//
// Suggested entities (from the ERP domain):
//   user, product, category, brand, unit, warehouse, stockLevel, stockMovement,
//   vendor, customer, purchase, sale, orderTicket, invoice, ledgerAccount,
//   ledgerEntry, cashTransaction, bankAccount, bankTransaction, companySetting.
//
// Example:
//   import { Schema, model } from 'mongoose';
//   const productSchema = new Schema({ name: String, sku: String /* … */ }, { timestamps: true });
//   export const Product = model('Product', productSchema);

export {};
